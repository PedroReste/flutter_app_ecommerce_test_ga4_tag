name: ecommerce_tagging_app
description: Protótipo Flutter de e-commerce para validação de tagueamento GA4.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.6.0
  firebase_analytics: ^11.3.3

  # Utilitários
  provider: ^6.1.2
  intl: ^0.19.0
  go_router: ^14.3.0
  uuid: ^4.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
